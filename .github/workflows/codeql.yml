name: codeql.yml

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: '* 9 * * *'

permissions:
  contents: read

jobs:
  codeql_java_backend:
    uses: Code-Syndicate-Latam/org-security/.github/workflows/codeql_java_backend.yml@main
    with:
      config-file: .github/codeql/codql-config.yml
