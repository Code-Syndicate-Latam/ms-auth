name: codeql.yml

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: '0 9 * * 0'

permissions: inherit

jobs:
  codeql_java_backend:
    uses: Code-Syndicate-Latam/org-security/.github/workflows/codeql_java_backend.yml@main
    with:
      secrets: inherit
      config-file: .github/codeql/codeql-config.yml
